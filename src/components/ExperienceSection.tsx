const steps = [["01", "Think", "Identify something worth exploring."], ["02", "Explore", "Look at the problem differently."], ["03", "Create", "Develop your unique idea."], ["04", "Share", "Present your thinking."], ["05", "Impact", "Turn possibility into something meaningful."]];

export default function ExperienceSection() {
  return <section className="experience section-pad" id="experience" aria-labelledby="experience-title"><div className="section-shell"><div className="section-index">02 <span>/</span> The experience</div><div className="experience-heading"><h2 id="experience-title">What happens<br />when you have<br /><em>an idea?</em></h2><p>There is no single way to think. IDEATHON is a space to follow your own thread — from the first spark to something you can share.</p></div><div className="journey" aria-label="The five steps of the Ideathon experience">{steps.map(([number, title, description], index) => <div className={`journey-step journey-step-${index + 1}`} key={number}><span className="journey-number">{number}</span><div><h3>{title}<span>.</span></h3><p>{description}</p></div>{index < steps.length - 1 && <span className="journey-arrow" aria-hidden="true">↓</span>}</div>)}</div></div></section>;
}
